Course Design Pattern
=====================

Note that some of the PDFs were directly edited with `PDF Reader` (removing signature of mine for security concern) so that exist some errors on the very first page.
